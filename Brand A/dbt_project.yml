name: 'aspen_dental'
version: '1.0.0'

require-dbt-version: [">={1.7.0}", "<2.0.0"]

profile: 'default'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

# ============================================
# MODEL CONFIGURATIONS
# ============================================
models:
  aspen_dental:
    staging:
      +materialized: view
      +schema: staging
      +tags: ['staging']

    intermediate:
      +materialized: view
      +schema: intermediate
      +tags: ['intermediate']

    marts:
      +materialized: table
      +schema: marts
      +tags: ['marts']
      +access: public
      +group: aspen_dental

    semantic:
      +materialized: table
      +tags: [semantic]

    example:
      +materialized: table

# ============================================
# SEED CONFIGURATIONS
# ============================================
seeds:
  aspen_dental:
    +schema: raw
    raw_billing:
      +column_types:
        charge_amount: NUMERIC
        insurance_paid: NUMERIC
        patient_paid: NUMERIC

# ============================================
# TEST CONFIGURATIONS
# ============================================
tests:
  +severity: warn

# ============================================
# VARS
# ============================================
vars:
  brand_name: 'Aspen Dental'
  brand_code: 'AD'
